{
  "name": "iz-play-v2.1",
  "version": "2.1.0",
  "private": true,
  "description": "IZ Play v2.1 - desktop, web, Android, painel, gateway e P2P",
  "scripts": {
    "app": "npm --prefix app start",
    "panel": "npm --prefix panel start",
    "gateway": "npm --prefix gateway start",
    "catalog-gateway": "npm --prefix catalog-gateway start",
    "web:preview": "node scripts/preview-web.js",
    "android:debug": "powershell -NoProfile -Command \"Set-Location android; .\\gradlew.bat assembleDebug\"",
    "check": "node scripts/check.js"
  }
}
